# Public URLs only. VITE_* values may be embedded in browser JavaScript.
# Axios REST endpoint, including /api:
VITE_API_BASE_URL=http://localhost:4000/api
# Existing clinical image links use an origin without /api:
VITE_API_URL=http://localhost:4000
# These are DEV examples, not a configuration for the future V3 deployment.
