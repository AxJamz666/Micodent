# RC4 uses same-origin /api and /uploads. No VITE_* configuration is required.
# Vite's development proxy is configured in vite.config.js.
# Never place credentials here: browser environment values are public.
