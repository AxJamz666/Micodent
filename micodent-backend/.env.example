# Template only. Never overwrite an existing installation's .env.
# Current S1-A code is deliberately restricted to this local DEV database/user.
PORT=4000
DB_HOST=localhost
DB_PORT=3306
DB_USER=dev_micodent
DB_NAME=micodent_dev
# Fill privately; blank values intentionally cannot start the backend.
DB_PASSWORD=
# Use an independently generated random secret; do not copy a shared example.
JWT_SECRET=
JWT_EXPIRES_IN=8h
